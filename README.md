# Viktor
a place to `add` your data references fast inside and get an `index` back
-> to remove it same at fast also later from your `Viktor` \o/
(nothing into your `memory allocation` needs to be `reorganized`.)

only some _optimizationTHING(^_^)_ ~~^^

  
## Installation
```
haxelib git viktor https://github.com/maitag/viktor.git
```

  
## Todo

- synopsis
- more types to store simple datatypes like `Int`
- more backends what using Bytes instead of Vector
- unit tests, benchmarks, hxp, optimization
  
  
